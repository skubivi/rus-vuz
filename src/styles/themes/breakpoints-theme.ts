import { createTheme } from "@mui/material";

export const breakpointsTheme = createTheme({
  breakpoints: {
    values: {
      xs: 320,
      sm: 480,
      md: 768,
      lg: 992,
      xl: 1200,
    },
  },
});
